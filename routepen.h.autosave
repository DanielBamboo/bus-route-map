#ifndef ROUTEPEN_H
#define ROUTEPEN_H

#include <QWidget>
#include <QPen>

class RoutePen : public QPen
{
    Q_OBJECT
public:
    RoutePen();
    //根据线路编号设置颜色，也许是一个hash函数
    void setColor(int route_num);
private:
    
};

#endif // ROUTEPEN_H
